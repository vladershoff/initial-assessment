package domain

import "errors"

// ErrShortCodeNotDefined возвращается, когда запрошенный короткий код ссылки
// отсутствует в хранилище.
//
// Используется в методах для сообщения, что запись не найдена, и корректной
// обработки (например, возврата HTTP 404).
var ErrShortCodeNotDefined = errors.New("не найден короткий код")
